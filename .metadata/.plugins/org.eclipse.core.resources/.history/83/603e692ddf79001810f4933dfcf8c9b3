package utils;

import javafx.animation.FadeTransition;
import javafx.util.Duration;

public class MyAnimation {
	public static void showOut() {
		FadeTransition ft = new FadeTransition(Duration.millis(500), textArea);
		ft.setFromValue(0.0);
		ft.setToValue(1.0);
		ft.play();
	}
}
